#include "main.h"
/**
 * _abs - give the absolut value of an int
 *@i: value to give modulo of it
 *Return: Always 0.
 */

int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}
}
